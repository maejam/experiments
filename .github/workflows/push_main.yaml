name: Push on main
on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - '*.rst'
      - '*.toml'
      - '*.yml'
      - '*.yaml'
      - '*.ini'

concurrency:
  group: push_main
  cancel-in-progress: true

jobs:

  tests:
    uses: ./.github/workflows/_tests.yaml

  release:
    needs: tests
    uses: ./.github/workflows/_release.yaml
